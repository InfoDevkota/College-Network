install dependencies -> npm install
run mongod -> sudo systemctl start mogod
run the server -> npm start

Good to go.


check Read-Me directory for API docs.

To run front end:-
    cd frontend
    quasar dev

    //TODO
    //Change the url in frontend/src/boot/axios.js
    //to the url of Backend

